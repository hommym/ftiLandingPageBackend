# Scheduler Secret Key
# IMPORTANT: Change this to a secure random string in production
# This key is used by the backup scheduler to authenticate and clear server memory
SCHEDULER_SECRET=your-secret-key-change-in-production
